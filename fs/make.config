include $(FSDIR)ramfs/make.config

FSROOT_OBJS=\
		  cache_node.o \
		  filesystem.o \
		  inode.o \
		  path.o \
		  vfs.o \

FS_OBJS=$(addprefix $(FSDIR),$(FSROOT_OBJS)) \
		$(addprefix $(FSDIR)ramfs/,$(FS_RAMFS_OBJS)) \
