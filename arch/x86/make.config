include $(ARCHDIR)boot/make.config
include $(ARCHDIR)drivers/make.config

export ARCH_ROOT_OBJS=gdt.o

ARCH_OBJS=$(addprefix $(ARCHDIR)boot/,$(ARCH_BOOT_OBJS)) \
		  $(addprefix $(ARCHDIR)drivers/,$(ARCH_DRIVERS_OBJS)) \
		  $(addprefix $(ARCHDIR),$(ARCH_ROOT_OBJS))
